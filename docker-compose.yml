version: '3.4'
services:
  resume:
    build:
      context: .
      args:
        LANG: en
    image: radtriste/resume/en:test
    ports:
      - "8080:80"
      
  cv:
    build:
      context: .
      args:
        LANG: fr
    image: radtriste/resume/fr:test
    ports:
      - "8081:80"